<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anochat_api.mapper.MessageMapper">
	<select id="getChatList" parameterType="int" resultType="com.anochat_api.dto.ChatListDto">
		SELECT 
			chat_id,
			chat_name,
			TO_CHAR(chat_date, 'yyyy/mm/dd') as chat_date,
			TO_CHAR(update_date, 'yyyy/mm/dd') as update_date
		FROM trn_chat
	</select>
</mapper>